# CodeQuest v55 Execution Architecture

Python projects execute in Pyodide. SQL projects use a fresh in-memory SQLite database. Web projects run inside a sandboxed iframe. Java remains a structural simulation until a secured server-side JVM runner is implemented.

Browser execution is educational and is not a secure general-purpose untrusted-code runner.
