# Voiceover Script - Generators

A generator produces values only when they are needed. Use `yield` instead of returning a complete list. This is useful for large files, streams and data pipelines. The mission builds a counting generator and a filtered generator for even values.
