export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.OPENAI_API_KEY)return res.status(503).json({error:'AI coach not configured',fallback:true});return res.status(501).json({error:'Connect approved server-side AI integration',fallback:true});}
