import crypto from "crypto";
export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const {academyId,projectId,files,mode="practice",assessmentId}=req.body||{};if(!academyId||!projectId||!files)return res.status(400).json({error:"Missing project context"});const projectHash=crypto.createHash("sha256").update(JSON.stringify(files)).digest("hex");if(!process.env.CLOUD_RUNNER_URL||!process.env.CLOUD_RUNNER_TOKEN)return res.status(503).json({error:"Cloud runner is not configured",fallback:mode==="practice",projectHash});return res.status(501).json({error:"Connect this contract to the isolated runner service",projectHash,mode,assessmentId});}
