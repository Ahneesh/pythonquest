export default async function handler(req,res){if(!["GET","POST"].includes(req.method))return res.status(405).json({error:"Method not allowed"});return res.status(501).json({error:"Connect this contract to the authenticated instructor review store"});}
