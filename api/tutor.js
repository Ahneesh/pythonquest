export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});if(!process.env.OPENAI_API_KEY)return res.status(503).json({error:"AI tutor is not configured"});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({model:process.env.OPENAI_TUTOR_MODEL||"gpt-5-mini",instructions:"You are an ADHD-friendly Python tutor. Give staged help and never reveal the full answer unless explicitly requested.",input:JSON.stringify(req.body),max_output_tokens:250})});const d=await r.json();return res.status(r.ok?200:502).json(r.ok?{message:d.output_text||"Try one smaller step."}:{error:"Tutor request failed"})}