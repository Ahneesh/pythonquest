# CodeQuest Engineering Lab

The Engineering Lab is a browser-based multi-file learning workspace.

It supports:

- academy-specific starter projects;
- multiple editable files;
- file switching;
- static project checks;
- snapshots;
- engineering reflection;
- JSON project export.

The checks are transparent static checks. They do not replace secure server-side execution,
real compilation or production-grade automated testing.
