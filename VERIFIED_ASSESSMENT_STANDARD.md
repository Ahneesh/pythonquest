# Verified Assessment Standard

Verified mode is timed, excludes AI help, keeps hidden tests server-side, permits limited visible checks, freezes the final submission and records a SHA-256 integrity hash plus review state.
